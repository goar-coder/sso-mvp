cambios en la consola de Keycloak han sido:

Authentication → Flows → browser → Verify Profile → puesto en Disabled
Realm Settings → User profile → email → Required field: OFF
Realm Settings → User profile → firstName → Required field: OFF
Realm Settings → User profile → lastName → Required field: OFF

Authentication → Required actions → desactiva verify Profile (OFF)
debe estar asi:
Configure OTP
├── Enabled: ON
└── Set as default action: OFF
Terms and Conditions
├── Enabled: OFF
└── Set as default action: Disabled OFF
Update Password
├── Enabled: ON
└── Set as default action: OFF
Update Profile
├── Enabled: ON
└── Set as default action: OFF
Verify Email
├── Enabled: ON
└── Set as default action: OFF
Delete Account
├── Enabled: OFF
└── Set as default action: Disabled OFF
Verify Profile
├── Enabled: OFF
└── Set as default action: Disabled OFF
Webauthn Register
├── Enabled: ON
└── Set as default action: OFF
Webauthn Register Passwordless
├── Enabled: ON
└── Set as default action: OFF
Delete Credential
├── Enabled: ON
└── Set as default action: OFF
Update User Locale
├── Enabled: ON
└── Set as default action: OFF

En Keycloak Admin:
Realm Settings → General → Frontend URL → ponlo en http://localhost:8080

En Realm roles crear: d1-access, d2-access, d3-access


Configure a new mapper → User Attribute
Name:               app-roles
User Attribute:     app_roles     ← custom attribute, escribe app_roles
Token Claim Name:   app_roles
Claim JSON Type:    String
Multivalued:        ON
Add to ID token:    ON
Add to access token: ON
Add to userinfo:    ON

Clients → d1-client → Client scopes → d1-client-dedicated → Add mapper → Configure a new mapper (by configuration) → User Property
Name:               preferred-username
Property:           username
Token Claim Name:   preferred_username  (ojo es under score no guion)
Claim JSON Type:    String
Add to ID token:    ON
Add to access token: ON
Add to userinfo:    ON


La misma configuración para d2-client y d3-client

Client scopes → roles → pestaña Mappers → click en realm roles →  Add to ID token: ON (obligatorio)
